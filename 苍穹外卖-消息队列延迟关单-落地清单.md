# 苍穹外卖 —— RabbitMQ 消息队列延迟关单改造方案

> 定位：用 **RabbitMQ 延迟消息（TTL + 死信交换机 DLX）** 替换现有的 `@Scheduled` 定时轮询，实现"下单后 15 分钟未支付自动关单"，并补上消息可靠性、消费者幂等两个面试必考点。
> 本方案只做"超时关单"这一个场景，做深做透，不铺开秒杀/多级缓存。

---

## 一、为什么要改（背景与收益）

### 现状
现有代码 `com.sky.task.OrderTask#processTimeoutOrder()`：

```java
@Scheduled(cron = "0 * * * * ?")   // 每分钟全表扫描一次
public void processTimeoutOrder() {
    LocalDateTime time = LocalDateTime.now().plusMinutes(-15);
    List<Orders> list = orderMapper.getByStatusAndOrderTimeLT(Orders.PENDING_PAYMENT, time);
    // 逐条 update 状态为已取消
}
```

### 痛点（面试时这样讲）
1. **时效性差**：定时任务是"每分钟扫一次"，关单时间最坏会延迟 1 分钟，且 15 分钟不精确。
2. **无效扫描 + 数据库压力大**：订单量上来后，每分钟一次 `status=1 and order_time < ...` 全表扫，量大时是空扫/重扫。
3. **扩展性差**：定时任务用单机 `@Scheduled`，多实例部署会重复执行或依赖分布式锁；把"扫描所有可能超时订单"集中在一个时刻，会造成瞬时流量尖峰。

### 改造后收益
- 每个订单**精确**在 15 分钟后被处理，不等轮询周期。
- 关单动作从"拉模型（扫描）"变成"推模型（消息触发）"，**削峰 + 解耦**。
- 为后续"下单异步化、催单、退款通知"等事件驱动改造打底。

---

## 二、技术选型与原理

- **组件**：RabbitMQ（通过 Spring Boot `spring-boot-starter-amqp`）。
- **延迟消息实现**：`TTL + 死信交换机（DLX）`，不装额外插件（官方延迟插件 `rabbitmq_delayed_message_exchange` 需要部署插件，实习生项目不必要）。

### 消息流转原理

```
用户下单（submitOrder）
      │  事务提交后发送消息(只发 orderId)
      ▼
order.delay.exchange (direct)  ──routingKey=order.delay──▶  order.delay.queue
                                                          (x-message-ttl = 15min
                                                           x-dead-letter-exchange = order.dlx.exchange
                                                           x-dead-letter-routing-key = order.close)
      │  消息在 delay 队列停留 15 分钟后过期 → 变成死信 → 按 DLX 转发
      ▼
order.dlx.exchange (direct)  ──routingKey=order.close──▶  order.close.queue
      │
      ▼
消费者 OrderCloseConsumer：查订单 → 若仍是"待付款"才取消 → 手动 ack（幂等）
```

**一句话记忆点**：消息先在一个带 TTL 的队列里"睡 15 分钟"，过期后被丢到死信交换机，再被路由到真正的关单队列，消费者只处理活人到期的订单。

---

## 三、依赖改动

`sky-take-out/sky-server/pom.xml` 新增（版本由 Spring Boot 2.7.3 的 parent 统一管理，无需写版本号）：

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-amqp</artifactId>
</dependency>
```

> 说明：项目 Spring Boot 版本为 2.7.3，`spring-boot-starter-amqp` 会自动带入对应版本的 `spring-rabbit`，不与现有依赖冲突。

---

## 四、配置改动

### 4.1 `application.yml`（`spring` 节点下新增）

```yaml
spring:
  rabbitmq:
    host: ${sky.rabbitmq.host}
    port: ${sky.rabbitmq.port}
    username: ${sky.rabbitmq.username}
    password: ${sky.rabbitmq.password}
    virtual-host: ${sky.rabbitmq.virtual-host}
    # 生产者消息确认（可靠性关键开关）
    publisher-confirm-type: correlated
    publisher-returns: true
    # 消费者手动确认
    listener:
      simple:
        acknowledge-mode: manual
```

### 4.2 `application-dev.yml`（`sky` 节点下新增占位符）

```yaml
sky:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    virtual-host: /
  order:
    timeout-minutes: 15   # 超时关单时长，可配置
```

> 现有订单超时是 15 分钟，这里抽成配置项 `sky.order.timeout-minutes`，比写死更规范，也是面试可讲的点。

---

## 五、表结构

**结论：orders 表不需要任何改动。**

理由：`orders` 表已经具备以下字段，原轮询代码也在用：
- `status`（订单状态，6=已取消）
- `cancel_reason`（取消原因）
- `cancel_time`（取消时间）

超时关单只更新这三列，且对象都是"待付款"订单（`pay_status = 0 未支付`），**无需退款**，所以不涉及 `pay_status` 变更。

### 可选进阶（本地消息表，用于"下单 + 发消息"的最终一致性）

如果要讲"如何避免下单成功但消息没发出去"的过硬方案，可新增一张本地消息表（这张表是唯一可选的新增）：

```sql
CREATE TABLE mq_message (
    id           BIGINT AUTO_INCREMENT PRIMARY KEY,
    message_id   VARCHAR(64)  NOT NULL COMMENT '消息唯一id',
    exchange     VARCHAR(128) NOT NULL,
    routing_key  VARCHAR(128) NOT NULL,
    body         TEXT         NOT NULL COMMENT '消息体(JSON)',
    status       TINYINT      NOT NULL DEFAULT 0 COMMENT '0待发送 1已发送 2发送失败',
    retry_count  INT          NOT NULL DEFAULT 0,
    create_time  DATETIME     NOT NULL,
    update_time  DATETIME     NOT NULL,
    UNIQUE KEY uk_message_id (message_id)
) COMMENT '本地消息表';
```

> **先不加这张表也可以**。基础方案用"事务提交后发送"（见 6.2），能解释清楚一致性即可；本地消息表 + 定时补偿，作为"进阶可靠性"在面试时口头说明即可。

---

## 六、代码结构

### 6.1 新建文件清单（都在 `sky-server` 模块）

```
com.sky
├─ config
│   └─ RabbitMQConfig.java          # 声明交换机/队列/绑定 + Jackson 转换器
├─ mq
│   ├─ OrderMqConstant.java         # 交换机、队列、routingKey 常量
│   ├─ OrderDelayProducer.java      # 生产者：发送延迟关单消息
│   └─ OrderCloseConsumer.java      # 消费者：监听关单队列并执行关单
```

### 6.2 `OrderMqConstant.java`（常量类）

```java
public final class OrderMqConstant {
    // 延迟交换机 / 队列
    public static final String DELAY_EXCHANGE = "order.delay.exchange";
    public static final String DELAY_QUEUE     = "order.delay.queue";
    public static final String DELAY_ROUTING_KEY = "order.delay";

    // 死信交换机 / 关单队列
    public static final String DLX_EXCHANGE    = "order.dlx.exchange";
    public static final String CLOSE_QUEUE     = "order.close.queue";
    public static final String CLOSE_ROUTING_KEY = "order.close";

    private OrderMqConstant() {}
}
```

### 6.3 `RabbitMQConfig.java`（核心：声明队列与绑定）

```java
@Configuration
public class RabbitMQConfig {

    // 延迟队列：设置 TTL 与死信转向
    @Bean
    public Queue delayQueue() {
        Map<String, Object> args = new HashMap<>();
        args.put("x-message-ttl", timeoutMinutes * 60 * 1000L);   // 毫秒
        args.put("x-dead-letter-exchange", OrderMqConstant.DLX_EXCHANGE);
        args.put("x-dead-letter-routing-key", OrderMqConstant.CLOSE_ROUTING_KEY);
        return new Queue(OrderMqConstant.DELAY_QUEUE, true, false, false, args);
    }

    // 关单队列（真正的消费队列）
    @Bean
    public Queue closeQueue() {
        return new Queue(OrderMqConstant.CLOSE_QUEUE, true);
    }

    @Bean
    public DirectExchange delayExchange() {
        return new DirectExchange(OrderMqConstant.DELAY_EXCHANGE, true, false);
    }

    @Bean
    public DirectExchange dlxExchange() {
        return new DirectExchange(OrderMqConstant.DLX_EXCHANGE, true, false);
    }

    // 绑定1：delay.exchange -> delay.queue
    @Bean
    public Binding delayBinding() {
        return BindingBuilder.bind(delayQueue()).to(delayExchange())
                .with(OrderMqConstant.DELAY_ROUTING_KEY);
    }

    // 绑定2：dlx.exchange -> close.queue
    @Bean
    public Binding closeBinding() {
        return BindingBuilder.bind(closeQueue()).to(dlxExchange())
                .with(OrderMqConstant.CLOSE_ROUTING_KEY);
    }

    // 消息用 JSON 序列化（替代默认 JDK 序列化，坑更少）
    @Bean
    public MessageConverter messageConverter() {
        return new Jackson2JsonMessageConverter();
    }
}
```

> 注意：`x-message-ttl` 若放在**队列级别**，则队列里所有消息延迟一致（关单场景都是 15 分钟，没问题）。如果将来不同订单要不同延迟，需改成**消息级别 TTL**（发送时 `MessageProperties.setExpiration`），并注意"队列 TTL vs 消息 TTL 的坑"——这是面试官爱追问的点，见第八节。

### 6.4 `OrderDelayProducer.java`（生产者）

```java
@Component
@Slf4j
public class OrderDelayProducer {

    @Autowired
    private RabbitTemplate rabbitTemplate;

    /**
     * 发送"超时关单"延迟消息。
     * 只发 orderId 即可，消费者按需查库拿最新状态。
     */
    public void sendCloseMessage(Long orderId) {
        // 用 orderId 作为 messageId，配合消费者幂等，也便于排查
        CorrelationData correlationData = new CorrelationData(String.valueOf(orderId));
        rabbitTemplate.convertAndSend(
            OrderMqConstant.DELAY_EXCHANGE,
            OrderMqConstant.DELAY_ROUTING_KEY,
            orderId,
            correlationData
        );
        log.info("已发送超时关单延迟消息，orderId={}", orderId);
    }
}
```

### 6.5 改 `OrderServiceImpl#submitOrder()`（接入发送点）

关键点：**发消息要放在"事务提交之后"**，否则会出现"事务回滚了、消息却已经发出去"的问题。

方案（推荐，简洁）：用 `TransactionSynchronizationManager` 注册 `afterCommit` 回调。

```java
@Transactional
public OrderSubmitVO submitOrder(OrdersSubmitDTO orderSubmitDTO) {
    // ...原有逻辑：校验地址、购物车、插入 orders、插入订单明细、清空购物车...

    Long orderId = orders.getId();

    // 事务提交后再发送延迟关单消息（避免事务回滚还发消息）
    TransactionSynchronizationManager.registerSynchronization(new TransactionSynchronization() {
        @Override
        public void afterCommit() {
            orderDelayProducer.sendCloseMessage(orderId);
        }
    });

    // ...原有 return ...
}
```

> 最小改动：只需在 `submitOrder` 末尾（`return` 之前）注入 `OrderDelayProducer` 并加这段回调，其余下单逻辑不动。

### 6.6 改 `OrderTask.java`（停用轮询）

- 注释或删除 `processTimeoutOrder()`（它被 MQ 消费者替代）。
- `processDeliveryOrder()`（处理"一直派送中"的订单）可选保留，不属于本方案范围。
- 保持 `@EnableScheduling`（如果只用了这一个定时任务且删除，可一并去掉，但为了不影响其他定时任务，先只注释本方法）。

### 6.7 `OrderCloseConsumer.java`（消费者，含幂等 + 状态校验）

```java
@Component
@Slf4j
public class OrderCloseConsumer {

    @Autowired
    private OrderMapper orderMapper;

    /**
     * 监听关单队列，将超时未支付的订单关闭。
     */
    @RabbitListener(queues = OrderMqConstant.CLOSE_QUEUE)
    public void onCloseOrder(Long orderId, Message message, Channel channel) throws IOException {
        long deliveryTag = message.getMessageProperties().getDeliveryTag();
        try {
            Orders orders = orderMapper.getById(orderId);
            if (orders == null) {
                // 订单不存在：直接确认，丢弃
                channel.basicAck(deliveryTag, false);
                return;
            }

            // 幂等 + 状态校验：只有"待付款"才关闭，已支付/已取消说明消息是重复的或已处理
            if (!Orders.PENDING_PAYMENT.equals(orders.getStatus())) {
                channel.basicAck(deliveryTag, false);   // 已支付/已取消，直接 ack
                return;
            }

            // 复用原轮询里的关单逻辑（对象是待付款，payStatus=0，无需退款）
            Orders update = Orders.builder()
                    .id(orders.getId())
                    .status(Orders.CANCELLED)
                    .cancelReason("订单超时自动取消")
                    .cancelTime(LocalDateTime.now())
                    .build();
            orderMapper.update(update);

            channel.basicAck(deliveryTag, false);
            log.info("订单超时自动关闭成功，orderId={}", orderId);
        } catch (Exception e) {
            log.error("关单失败，orderId={}", orderId, e);
            // 消费失败：重新入队或进入死信，简单做法是 basicNack 携 requeue=true 重投
            channel.basicNack(deliveryTag, false, true);
        }
    }
}
```

---

## 七、消息可靠性与幂等（面试必考，逐条背）

| 问题 | 方案 | 对应清单位置 |
|---|---|---|
| 消息不丢（生产者→Broker） | `publisher-confirm-type: correlated` + `CorrelationData` 确认回调 | 4.1 / 6.4 |
| 消息不丢（Broker 侧） | 交换机、队列、消息都持久化（`durable=true`） | 6.3 |
| 消息不丢（消费者→Broker） | `acknowledge-mode: manual` 手动 ack，处理成功再 ack | 4.1 / 6.7 |
| 消费者重复消费 | 幂等：消费前按 orderId 查最新状态，非"待付款"直接 ack 跳过 | 6.7 |
| 下单成功但消息没发 | 事务提交后发送（`afterCommit`）；进阶用本地消息表 + 定时补偿 | 6.5 / 5.1 |
| 消息顺序 | 关单场景按 orderId 无顺序要求，天然不需保证顺序 | 面试可主动说明 |

---

## 八、面试话术

### STAR 一句话介绍（2 分钟内）

> "苍穹外卖原本用 `@Scheduled` 每分钟全表扫描实现『下单 15 分钟未支付自动取消』，存在时效性差、无效扫描、瞬时尖峰三个问题。我改成了 RabbitMQ 延迟消息：下单事务提交后发送一条只包含 orderId 的消息，进入一个设置了 15 分钟 TTL 的延迟队列，过期后被投递到死信交换机再路由到关单队列，由消费者精确关单。为了保证可靠性，我开启了生产者 confirm 确认、交换机和队列持久化、消费者手动 ack，并用『消费前先查订单状态、只有待付款才关』实现幂等，避免重复消费。最终把单机轮询改成了事件驱动，实现了精确关单、削峰解耦。"

### 高频追问预演

1. **"RabbitMQ 怎么做延迟消息？"**
   → TTL + 死信交换机。消息先发到一个普通队列，队列设 `x-message-ttl`，消息不消费、到期后变成死信（dead letter），按队列上的 `x-dead-letter-exchange` / `x-dead-letter-routing-key` 转发到另一个交换机，再进关单队列被真正消费。

2. **"延迟消息的坑有哪些？"**
   → 队列级 TTL 是整队统一延迟，且**死信只从队头出**——如果队列里既有 TTL 短又有 TTL 长的消息，队头的长 TTL 消息会阻塞后面短 TTL 消息，导致短消息反而后到。关单场景所有消息都是 15 分钟，不存在此问题；若需不同延迟，要用**消息级 TTL**或官方延迟插件。

3. **"怎么保证消息不丢？"**
   → 三段式：生产端 confirm 确认（`publisher-confirm-type: correlated`），Broker 端交换机/队列/消息全持久化，消费端手动 ack、成功才确认。外加"下单事务提交后才发消息"避免事务回滚丢消息。

4. **"消费者重复消费怎么办？怎么保证幂等？"**
   → 消息本身带 orderId，消费时先 `getById` 查最新状态，只有 `status == 待付款` 才执行关单，已支付/已取消直接 ack 跳过。这样即使消息重复投递，结果也一致。

5. **"下单事务和发消息怎么保证一致？"**
   → 我在事务内部只做 DB 写入，通过 `TransactionSynchronizationManager` 在 `afterCommit` 回调里发消息，保证"只有事务提交成功才发"。如果还要更强的保障，会用本地消息表 + 定时任务补偿（半消息/最终一致性）。

6. **"为什么不用定时任务了？它有什么不好？"**
   → 轮询是"拉"，时效差（最多晚一个周期）、多次无效扫库、且单机 `@Scheduled` 多实例会重复执行；MQ 是"推"，精确到期触发、削峰解耦，符合事件驱动。

7. **"只发 orderId，消费者再查库，会不会有不一致？"**
   → 反而更安全。发完整快照可能拿到过期数据；只发 id、消费时查库，能拿到订单最新状态，正好服务于幂等判断。

---

## 九、落地步骤清单（按顺序做）

1. 本地安装/启动 RabbitMQ（Docker 一条命令即可：`docker run -d --name rabbitmq -p 5672:5672 -p 15672:15672 rabbitmq:3-management`）。
2. `pom.xml` 加 `spring-boot-starter-amqp`。
3. `application.yml` + `application-dev.yml` 加 RabbitMQ 与 `timeout-minutes` 配置。
4. 新建 `mq` 包三个类 + `RabbitMQConfig`，按 6.2~6.4、6.7 落地。
5. `submitOrder()` 接入 `afterCommit` 发送。
6. 注释 `OrderTask#processTimeoutOrder()`。
7. 启动服务，验证队列/交换机在 RabbitMQ 管理台（15672）正确创建。
8. 验证：下单不支付 → 15 分钟后自动"已取消"；下单并支付 → 不被关单；手动重发同 orderId 消息 → 幂等不重复处理。

---

## 十、常见坑提醒

- **默认序列化是 JDK**：不配 `Jackson2JsonMessageConverter` 时消息体是 JDK 序列化字节，跨语言/可读性都差，务必配 JSON 转换器。
- **队列名别混**：`delay.queue` 是"睡觉"的，`close.queue` 才是消费者监听的，`@RabbitListener` 要绑 `close.queue`。
- **别在事务内直接发消息**：会 "事务回滚但消息已飞出去"，用 `afterCommit`。
- **手动 ack 别忘**：`manual` 模式下处理完必须 `basicAck`，否则消息会一直 unacked 堆积。
- **`x-message-ttl` 单位是毫秒**：配置 `timeout-minutes * 60 * 1000` 才正确。