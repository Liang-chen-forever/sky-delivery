# 苍穹外卖 —— RabbitMQ 消息队列延迟关单改造方案

> 定位：用 **RabbitMQ 延迟消息（TTL + 死信交换机 DLX）** 替换现有的 `@Scheduled` 定时轮询，实现"下单后 15 分钟未支付自动关单"，并补上消息可靠性、消费者幂等两个面试必考点。
> 本方案只做"超时关单"这一个场景，做深做透，不铺开秒杀/多级缓存。
>
> **当前落地状态（2026-09-11）**：核心代码、配置、outbox 建表脚本、原子关单 SQL 和 16 个回归测试已落地。实现以 docs/superpowers/specs/2026-09-10-order-close-messaging-design.md 为准：下单事务写入 outbox，由 relay 发布并等待 confirm；不再使用 afterCommit 直发，也不采用“先查询再更新”的非原子关单。
>
> **验收边界**：Maven 功能测试不依赖 RabbitMQ/MySQL；真实链路仍需先执行 docs/sql/order-close-outbox.sql 并启动 RabbitMQ，再按第九节进行端到端验证。

---

## 一、为什么要改（背景与收益）

### 现状
现有代码 `com.sky.task.OrderTask#processTimeoutOrder()`：

```java
@Scheduled(cron = "0 * * * * ?")   // 每分钟全表扫描一次
public void processTimeoutOrder() {
    LocalDateTime time = LocalDateTime.now().plusMinutes(-15);
    List<Orders> list = orderMapper.getByStatusAndOrderTimeLT(Orders.PENDING_PAYMENT, time);
    // 逐条 update 状态为已取消
}
```

### 痛点（面试时这样讲）
1. **时效性差**：定时任务是"每分钟扫一次"，关单时间最坏会延迟 1 分钟，且 15 分钟不精确。
2. **无效扫描 + 数据库压力大**：订单量上来后，每分钟一次 `status=1 and order_time < ...` 全表扫，量大时是空扫/重扫。
3. **扩展性差**：定时任务用单机 `@Scheduled`，多实例部署会重复执行或依赖分布式锁；把"扫描所有可能超时订单"集中在一个时刻，会造成瞬时流量尖峰。

### 改造后收益
- 每个订单**精确**在 15 分钟后被处理，不等轮询周期。
- 关单动作从"拉模型（扫描）"变成"推模型（消息触发）"，**削峰 + 解耦**。
- 为后续"下单异步化、催单、退款通知"等事件驱动改造打底。

---

## 二、技术选型与原理

- **组件**：RabbitMQ（通过 Spring Boot `spring-boot-starter-amqp`）。
- **延迟消息实现**：`TTL + 死信交换机（DLX）`，不装额外插件（官方延迟插件 `rabbitmq_delayed_message_exchange` 需要部署插件，实习生项目不必要）。

### 消息流转原理

```
用户下单（submitOrder）
      │  事务提交后发送消息(只发 orderId)
      ▼
order.delay.exchange (direct)  ──routingKey=order.delay──▶  order.delay.queue
                                                          (x-message-ttl = 15min
                                                           x-dead-letter-exchange = order.dlx.exchange
                                                           x-dead-letter-routing-key = order.close)
      │  消息在 delay 队列停留 15 分钟后过期 → 变成死信 → 按 DLX 转发
      ▼
order.dlx.exchange (direct)  ──routingKey=order.close──▶  order.close.queue
      │
      ▼
消费者 OrderCloseConsumer：查订单 → 若仍是"待付款"才取消 → 手动 ack（幂等）
```

**一句话记忆点**：消息先在一个带 TTL 的队列里"睡 15 分钟"，过期后被丢到死信交换机，再被路由到真正的关单队列，消费者只处理活人到期的订单。

---

## 三、依赖改动

`sky-take-out/sky-server/pom.xml` 新增（版本由 Spring Boot 2.7.3 的 parent 统一管理，无需写版本号）：

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-amqp</artifactId>
</dependency>
```

> 说明：项目 Spring Boot 版本为 2.7.3，`spring-boot-starter-amqp` 会自动带入对应版本的 `spring-rabbit`，不与现有依赖冲突。

---

## 四、配置改动

### 4.1 `application.yml`（`spring` 节点下新增）

```yaml
spring:
  rabbitmq:
    host: ${sky.rabbitmq.host}
    port: ${sky.rabbitmq.port}
    username: ${sky.rabbitmq.username}
    password: ${sky.rabbitmq.password}
    virtual-host: ${sky.rabbitmq.virtual-host}
    # 生产者消息确认（可靠性关键开关）
    publisher-confirm-type: correlated
    publisher-returns: true
    # 消费者手动确认
    listener:
      simple:
        acknowledge-mode: manual
```

### 4.2 `application-dev.yml`（`sky` 节点下新增占位符）

```yaml
sky:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    virtual-host: /
  order:
    timeout-minutes: 15   # 超时关单时长，可配置
```

> 现有订单超时是 15 分钟，这里抽成配置项 `sky.order.timeout-minutes`，比写死更规范，也是面试可讲的点。

---

## 五、表结构

**结论：orders 表不需要任何改动。**

理由：`orders` 表已经具备以下字段，原轮询代码也在用：
- `status`（订单状态，6=已取消）
- `cancel_reason`（取消原因）
- `cancel_time`（取消时间）

超时关单只更新这三列，且对象都是"待付款"订单（`pay_status = 0 未支付`），**无需退款**，所以不涉及 `pay_status` 变更。

### 5.1 事务性本地消息表（实际采用）

本次落地采用 outbox，而不是把消息表作为可选项。订单写入和 order_close_outbox 写入在同一个 @Transactional 方法中完成，数据库事务提交后由 relay 负责发布和重试。这样可以覆盖“订单已提交、应用在发消息前宕机”的丢消息窗口。

建表脚本位于 docs/sql/order-close-outbox.sql，包含：

- uk_message_id：消息唯一标识；
- uk_order_id：一单一条关单消息；
- (status, next_retry_time, id)：relay 查询待发送记录的索引；
- retry_count、last_error：发布失败的重试记录。

> 运行服务前必须先执行建表脚本。outbox 记录发布成功后保留在表中，后续可按保留策略归档或清理。

---

## 六、代码结构

### 6.1 实际文件清单

~~~text
sky-pojo
└─ src/main/java/com/sky/entity/OrderCloseOutbox.java

sky-server
├─ src/main/java/com/sky/config/RabbitMQConfig.java
├─ src/main/java/com/sky/mq/
│  ├─ OrderMqConstant.java
│  ├─ OrderCloseProducer.java
│  ├─ OrderCloseMessagePublisher.java
│  ├─ OrderCloseOutboxRelay.java
│  └─ OrderCloseConsumer.java
├─ src/main/java/com/sky/service/
│  ├─ OrderCloseService.java
│  └─ impl/OrderCloseServiceImpl.java
├─ src/main/java/com/sky/mapper/OrderCloseOutboxMapper.java
└─ src/main/resources/mapper/OrderCloseOutboxMapper.xml
~~~

### 6.2 RabbitMQ 拓扑和配置

RabbitMQConfig 声明持久化的延迟交换机、延迟队列、关单交换机、关单队列和绑定：

- order.delay.queue：队列级 TTL，默认 15 分钟；
- TTL 到期后通过 x-dead-letter-exchange=order.close.exchange 和 routing key order.close 转发；
- order.close.queue：消费者真正监听的队列；
- Jackson2JsonMessageConverter：消息体只传 orderId，使用 JSON 序列化。

TTL 由 sky.order.timeout-minutes 控制。生产端开启 correlated publisher confirm 和 return，消费端使用 manual ack。

### 6.3 Outbox 写入和 relay 发布

OrderServiceImpl#submitOrder 已有 @Transactional，订单插入后立即写入 outbox：

~~~java
orderMapper.insert(orders);
orderCloseProducer.createMessage(orders.getId());
~~~

OrderCloseProducer 只负责写入发布意图；OrderCloseOutboxRelay 每秒按批次查询到期的待发送记录，调用 OrderCloseMessagePublisher 发布消息并等待 confirm：

- confirm 成功：将记录标记为 status=1；
- nack、超时或 returned message：增加 retry_count，记录 last_error，按重试间隔再次投递；
- relay 扫描允许重复发布，消费者通过条件更新保证重复消息无副作用。

### 6.4 原子关单

OrderCloseServiceImpl 调用 OrderMapper.closeIfPending，对应 SQL 必须是单条条件更新：

~~~sql
update orders
set status = 6,
    cancel_reason = '支付超时，自动取消',
    cancel_time = ?
where id = ?
  and status = 1
  and pay_status = 0;
~~~

影响行数为 1 表示完成关单，0 表示订单已支付、已取消或不存在。单条 UPDATE 让支付回调和延迟关单在数据库行锁下安全竞争，避免“先查询再更新”的误取消。

### 6.5 消费者 ack 策略

OrderCloseConsumer 只在关单服务正常返回后 basicAck。影响行数为 0 仍是正常幂等结果；数据库等异常则 basicNack(..., requeue=true)，等待基础设施恢复后重试。

### 6.6 定时任务边界

- 已删除 OrderTask#processTimeoutOrder()，不再扫描待支付订单；
- 保留 processDeliveryOrder()，它负责派送中订单的每日兜底完成；
- @EnableScheduling 和 outbox relay 仍然保留。

---

## 七、消息可靠性与幂等（面试必考，逐条背）

| 问题 | 方案 | 对应清单位置 |
|---|---|---|
| 消息不丢（生产者→Broker） | `publisher-confirm-type: correlated` + `CorrelationData` 确认回调 | 4.1 / 6.4 |
| 消息不丢（Broker 侧） | 交换机、队列、消息都持久化（`durable=true`） | 6.3 |
| 消息不丢（消费者→Broker） | `acknowledge-mode: manual` 手动 ack，处理成功再 ack | 4.1 / 6.7 |
| 消费者重复消费 | 幂等：单条条件 UPDATE 只允许 status=1 且 pay_status=0 的订单被取消，影响行数为 0 时直接 ack | 6.7 |
| 下单成功但消息没发 | 订单与 outbox 意图同事务提交；relay 发布确认失败后记录错误并重试 | 5.1 / 6.3 |
| 消息顺序 | 关单场景按 orderId 无顺序要求，天然不需保证顺序 | 面试可主动说明 |

---

## 八、面试话术

### STAR 一句话介绍（2 分钟内）

> "苍穹外卖原本用 `@Scheduled` 每分钟全表扫描实现『下单 15 分钟未支付自动取消』，存在时效性差、无效扫描、瞬时尖峰三个问题。我改成了 RabbitMQ 延迟消息：下单事务内写入 outbox，relay 发布一条只包含 orderId 的消息，进入一个设置了 15 分钟 TTL 的延迟队列，过期后被投递到死信交换机再路由到关单队列，由消费者精确关单。为了保证可靠性，我开启了生产者 confirm 确认、交换机和队列持久化、消费者手动 ack，并用『status=1 且 pay_status=0 的原子条件 UPDATE』实现幂等，避免重复消费。最终把单机轮询改成了事件驱动，实现了精确关单、削峰解耦。"

### 高频追问预演

1. **"RabbitMQ 怎么做延迟消息？"**
   → TTL + 死信交换机。消息先发到一个普通队列，队列设 `x-message-ttl`，消息不消费、到期后变成死信（dead letter），按队列上的 `x-dead-letter-exchange` / `x-dead-letter-routing-key` 转发到另一个交换机，再进关单队列被真正消费。

2. **"延迟消息的坑有哪些？"**
   → 队列级 TTL 是整队统一延迟，且**死信只从队头出**——如果队列里既有 TTL 短又有 TTL 长的消息，队头的长 TTL 消息会阻塞后面短 TTL 消息，导致短消息反而后到。关单场景所有消息都是 15 分钟，不存在此问题；若需不同延迟，要用**消息级 TTL**或官方延迟插件。

3. **"怎么保证消息不丢？"**
   → 三段式：生产端 confirm 确认（`publisher-confirm-type: correlated`），Broker 端交换机/队列/消息全持久化，消费端手动 ack、成功才确认。外加"订单与 outbox 同事务提交，relay 可恢复发布"避免事务回滚或宕机导致消息意图丢失。

4. **"消费者重复消费怎么办？怎么保证幂等？"**
   → 消费者执行带 status=1、pay_status=0 条件的单条 UPDATE，影响行数为 0 就 ack。支付回调和关单更新由数据库行锁串行化，重复消息不会重复取消。

5. **"下单事务和发消息怎么保证一致？"**
   → 我在下单事务内同时写订单和 outbox，事务成功后由 relay 发布并等待 publisher confirm；失败记录重试，避免 afterCommit 发送窗口。

6. **"为什么不用定时任务了？它有什么不好？"**
   → 轮询是"拉"，时效差（最多晚一个周期）、多次无效扫库、且单机 `@Scheduled` 多实例会重复执行；MQ 是"推"，精确到期触发、削峰解耦，符合事件驱动。

7. **"只发 orderId，消费者再查库，会不会有不一致？"**
   → 反而更安全。发完整快照可能拿到过期数据；只发 id、消费时查库，能拿到订单最新状态，正好服务于幂等判断。

---

## 九、落地与验收清单

### 9.1 已完成的代码与配置

- [x] spring-boot-starter-amqp 已加入 sky-server/pom.xml。
- [x] RabbitMQ 连接、publisher confirm、returns、manual ack 和 prefetch 已加入 application.yml。
- [x] application-dev.example.yml 已提供 RabbitMQ 和全部 sky.order.* 参数的环境变量模板。
- [x] 已声明持久化 exchange、TTL 延迟队列、DLX/关单队列、绑定和 JSON converter。
- [x] submitOrder() 已在订单插入后、同一事务内写入 outbox。
- [x] outbox relay 已实现有界批量发布、publisher confirm、失败记录和延迟重试。
- [x] 待支付扫描已从 OrderTask 移除，派送中订单任务保留。
- [x] 关单已改为 status=1 and pay_status=0 的原子条件 UPDATE。
- [x] 消费者已实现成功 ack、业务幂等 ack、异常 nack 重入队。
- [x] docs/sql/order-close-outbox.sql 和 16 个功能回归测试已纳入提交范围。

### 9.2 本地端到端验收

1. 执行 docs/sql/order-close-outbox.sql，确认 order_close_outbox 表存在。
2. 启动 RabbitMQ：docker run -d --name rabbitmq -p 5672:5672 -p 15672:15672 rabbitmq:3-management。
3. 启动后端并下单不支付：确认 outbox 记录变为 status=1，15 分钟后订单变为已取消。
4. 下单后完成支付：确认延迟消息到达消费者时条件 UPDATE 影响 0 行，订单保持已支付状态。
5. 重复投递同一 orderId：确认消费者正常 ack 且订单不会重复修改。
6. 暂停 RabbitMQ 或制造数据库异常：确认 relay/消费者不会静默丢消息，恢复后能够重试。

### 9.3 自动化验证

功能测试命令：

~~~bash
mvn -pl sky-server -am -Dtest=RabbitMQConfigTest,OrderCloseOutboxSqlContractTest,OrderCloseOutboxRelayTest,OrderCloseMessagePublisherTest,OrderCloseProducerTest,OrderCloseSqlContractTest,OrderCloseServiceImplTest,OrderCloseConsumerTest,OrderServiceImplOutboxContractTest -DfailIfNoTests=false test
~~~

该命令不需要 RabbitMQ 或 MySQL。项目原有的 HttpClientTest 和 SpringDataRedisTest 依赖外部服务/未初始化注入，不属于本功能验收范围。

---

## 十、常见坑提醒

- outbox 表不是自动迁移：启动服务前必须执行 docs/sql/order-close-outbox.sql。
- 队列名别混：order.delay.queue 负责等待 TTL，order.close.queue 才是消费者监听的队列。
- 不要用 afterCommit 直发代替 outbox：提交后、发送前宕机会丢失关单消息意图。
- 不要先查后改：关单必须使用带 status 和 pay_status 条件的单条 UPDATE。
- 手动 ack 别忘：成功和幂等跳过都要 ack，数据库异常要 nack 重入队。
- x-message-ttl 单位是毫秒：timeout-minutes * 60 * 1000 才正确。
- 队列级 TTL 只适合单一超时档位：将来出现多档延迟时，应拆分多条 TTL 队列或改用延迟插件。
- outbox 需要清理策略：已发送记录长期增长，应按保留期归档或删除。
